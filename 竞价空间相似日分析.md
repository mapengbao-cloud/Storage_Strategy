# 竞价空间相似日分析

## 概述

基于场站竞价空间曲线的 96 点形态，通过多维相似度算法找到与目标日最相似的历史日期。相似日分析是电价预测和交易策略的核心输入：**相似日的历史电价走势可作为目标日日前电价预测的参考依据**。

**脚本：** `06 DataMining/similar_day_analysis.py`

**数据源：** 本地 SQLite `data/cache/local.db`

**输出：** `output/竞价空间分析结果/相似日分析_YYYY-MM-DD.html`

---

## 一、使用流程

```bash
cd "06 DataMining"

# 第1步：同步天机数据（竞价空间 + 电价）
python local_db.py sync --tables bidding_space,price --start YYYY-MM-DD --end YYYY-MM-DD

# 第2步：重算所有日期的特征
python similar_day_analysis.py compute

# 第3步：生成目标日相似日分析 HTML
python similar_day_analysis.py 2026-07-07
python similar_day_analysis.py 2026-07-07 --top 15  # 指定 Top N
```

> **注意：** 每次生成前都应先同步数据 + 重算特征，确保包含最新日期的竞价空间和价格。

---

## 二、数据源

### 竞价空间数据

| 数据 | 本地表 | 天机源表 | 说明 |
|------|--------|---------|------|
| 预测竞价空间 | `bidding_space_forecast` | `shandong_px_spot_dayahead_load_info` | 96点，用于相似度计算 |
| 实际竞价空间 | `bidding_space_actual` | `shandong_px_spot_actual_load_info` | 96点，用于 HTML 图表叠加 |

> 竞价空间 = 直调负荷 − (联络线受电 + 风电 + 光伏 + 核电 + 自备机组)（本地同步时计算）

### 电价数据

| 曲线 | 本地表 | 数据来源 | 日期范围 |
|------|--------|---------|---------|
| 日前电价 | `dayahead_price` | 天机 + ADS 齐河站补齐 | 2023-02-04 ~ 至今 |
| 实时电价 | `realtime_price` | 天机 + ADS 齐河站补齐 | 2023-02-04 ~ 至今 |

> 电价仅用于 HTML 图表展示，不参与相似度计算。相似度仅基于竞价空间曲线。

---

## 三、相似度算法（7 维度加权 + 季节 Bonus）

### 特征提取

对每个日期的 96 点竞价空间曲线：

1. **2h 滑动窗口**（8 个 15 分钟点）：遍历所有 96 点窗口，找最高/最低均值窗口
2. **时段分类**：凌晨(0-5)、上午(6-11)、中午(12-13)、下午(14-17)、晚间(18-23)
3. **Pearson 相关系数**：两条 96 点曲线的线性相关度
4. **时间近邻**：候选日与目标日相距天数（越近越可靠）
5. **谷段现货负价**：候选日谷值时段2h的日前电价<0比例

### 7 维度权重

| 维度 | 权重 | 计算方式 | 物理意义 |
|------|------|---------|---------|
| 2h 谷值 | 25% | `1 - abs(候选谷值 - 目标谷值) / max(所有谷值)` | 谷值越接近，充电需求越一致 |
| 2h 峰值 | 20% | `1 - abs(候选峰值 - 目标峰值) / max(所有峰值)` | 峰值越接近，保供水平越一致 |
| 谷值时段 | 12% | 同段=1.0，邻段=0.5，跨段=0.0 | 谷值落在同时段 |
| 峰值时段 | 8% | 同上 | 峰值落在同时段 |
| 曲线形状 | 15% | Pearson 相关系数 | 96点形态拟合度 |
| **时间近邻** | **10%** | 2周内→1.0，15-30天→0.5→1.0，31-60天→0.3，去年→0.2 | 越近越可靠（机组组合/规则近期变化小） |
| **谷段负价** | **10%** | 候选日谷段2h日前价<0比例 | 负价=优先充电机会（套利明确） |

**基础得分 = 0.25×谷值 + 0.20×峰值 + 0.12×谷值时段 + 0.08×峰值时段 + 0.15×曲线形状 + 0.10×时间近邻 + 0.10×谷段负价**

> 注：实时价差维度已取消（无数据支撑），原4%权重并入谷段负价（6%→10%）。电价信号维度需要候选日有历史日前电价数据，缺失时该维度记0分（不惩罚无电价日）。时间近邻确保优先用近期数据（2周内最可靠），避免用陈旧数据误导。

---

## 四、季节权重体系

电力系统季节特性决定机组开机组合，不同季节的竞价空间形态有本质差异。相似日优先选择同季节、同保供特性的历史日期。

### 季节划分

| 季节 | 月份范围 | 保供尖峰 | 说明 |
|------|---------|---------|------|
| 夏季 | 6-8月 | **7-8月** | 空调尖峰保供 |
| 供暖季 | 11/15 ~ 3/15 | **12-2月** | 供暖尖峰保供 |
| 春季 | 3/16 ~ 5/31 | — | 过渡季节 |
| 秋季 | 9/1 ~ 11/14 | — | 过渡季节 |

### 保供尖峰月份 (7-8月, 12-2月)

保供月份机组开机方式特殊，同保供月份的数据最具参考价值。

| 优先级 | 条件 | Bonus | 标签 |
|--------|------|-------|------|
| 1 | 本年同保供月份 ±30天 | **+0.12** | [本年保供] |
| 2 | 去年同保供月份 | +0.10 | [去年保供] |
| 3 | 本年同季节非保供（6月 / 11/15-30, 3/1-15） | +0.08 | [本年同季] |
| 4 | 去年同季节非保供 | +0.06 | [去年同季] |
| 5 | 本年 ±30天（跨季节） | +0.04 | [本年30日] |

**其他日期全部排除，不参与排序。**

### 非保供季节 (6月, 春季, 秋季, 供暖非尖峰)

| 优先级 | 条件 | Bonus | 标签 |
|--------|------|-------|------|
| 1 | 去年同季节 | +0.08 | [去年同季] |
| 2 | 本年同季节 ±30天 | +0.06 | [本年同季] |
| 3 | 本年 ±30天 | +0.04 | [本年30日] |

**其他日期全部排除，不参与排序。**

### 最终得分

**最终得分 = 基础得分 + 季节 Bonus**

---

## 五、HTML 输出内容

### 图表 1：目标日竞价空间 + 电价

- 目标日预测竞价空间（蓝色实线 + 面积填充）
- 目标日实际竞价空间（绿色虚线）
- 点击排名表行 → 叠加该相似日的预测竞价空间（黄色点线）+ 实际竞价空间（红色虚线）
- 电价（右轴）：日前电价（紫色）、实时电价（红色）——显示目标日或高亮相似日的电价
- 鼠标悬停显示 96 点精确数值

### 图表 2：相似日曲线叠加对比

- 目标日预测竞价空间（蓝色粗线）+ 实际竞价空间（绿色虚线）
- 各相似日预测竞价空间（彩色细线，点击高亮变粗）
- 高亮相似日的实际竞价空间（红色虚线）
- 图例可滚动

### 图表 3：雷达图

- 8 维度相似度可视化（2h谷值、2h峰值、谷值时段、峰值时段、曲线形状、时间近邻、谷段负价、实时价差）
- 目标日 = 满值 1.0，各相似日按维度着色

### 排名表

- Top N 相似日，含日期、相似度得分、2h谷值、2h峰值、峰谷差、曲线相关性
- 标注季节标签（[本年保供]、[去年保供] 等）
- 点击行高亮对应曲线，再次点击取消

### 侧边栏统计

- 目标日竞价空间均值、2h谷值/峰值、季节标签
- 算法说明

### 储能充放推荐面板（新增）

位于摘要栏下方，基于目标日 2h 谷值/峰值 → 火电开机台数回归 + **双变量判据**，给出次日储能+抽蓄充放建议：

#### 双变量判据：bs 谷值 × 火电峰值台数

| 判据 | 作用 | 物理机制 |
|------|------|---------|
| **主判据：bs 2h谷值** | 决定触地板概率（谷值越低→光伏淹没负荷越深→100%触地板） | 谷值<0→98.6%触地板；谷值>20GW→仅11% |
| **辅判据：火电峰值台数** | 决定谷段最小出力地板线高度 | 开停机有费用→为保峰值供应少停机→峰值台数越多→谷段最小出力被抬高 |

**峰值台数 → 谷段最小出力地板线（关键约束）**：

| 峰值台数 | 谷段最小出力(均值) | 触地板率 |
|---------|------------------|---------|
| <60 | 7856 MW | 100% |
| 70-80 | 11193 MW | 96% |
| 80-90 | 12733 MW | 96% |
| 90-100 | 17035 MW | 70% |
| >100 | 22421 MW | 56% |

**触地板概率矩阵（bs谷值 × 峰值台数）**：谷值<0 无论台数100%触地板；谷值>20GW且台数>90仅29-34%；中间区间10-20GW靠峰值台数修正（少开机可深压→触地板概率高，保供多开机压不下去→概率低）。

#### 推荐项

| 推荐项 | 计算逻辑 |
|--------|---------|
| 谷段充电功率 | 主由谷值深度分档；中间区间（10-20GW）用峰值台数修正：台数>100（保供）降一档，台数≤80（少开机）升一档 |
| 峰段放电功率 | 由峰值台数分档：台数越多（保供挤占）→放电略降 |
| 峰值/谷值火电台数 | 回归：`峰值台数≈0.0019×bs_peak+13.12`（R²=0.65）；`谷值台数≈0.0011×bs_valley+79.88`（R²=0.55） |
| 谷段最小出力地板线 | 由峰值台数查表（7856~22421 MW） |
| 触地板概率 | 双变量矩阵查表 |
| 2h 能量估算 | 充电能量=|充电功率|×2h；放电能量=充电能量×0.89 |

**逻辑链**：次日预测bs峰值→火电峰值台数（开停机决策）→峰值台数决定谷段最小出力地板线→bs谷值 vs 地板线：谷值<地板线→火电压到最小出力→触-80地板价→储能必投充电；谷值>地板线→火电无需压到最小→无地板价→储能弱投。

### 日期跳转

- 下拉框选择任意日期 → 跳转到对应日期的相似日分析 HTML

---

## 六、依赖数据检查清单

生成相似日分析前，确保以下数据就绪：

| 检查项 | 命令 | 预期 |
|--------|------|------|
| 竞价空间已同步 | `python local_db.py sync --tables bidding_space` | 含目标日 forecast + actual |
| 电价已同步 | `python local_db.py sync --tables price` | 含目标日及候选日 |
| 特征已计算 | `python similar_day_analysis.py compute` | 含目标日特征 |

---

## 七、特征存储

`_tmp_similar_features.json`（项目根目录）：

- 仅存统计量（峰谷值、时段、时间），不含完整 96 点曲线
- 完整曲线在生成 HTML 时从 `bidding_space_forecast` 实时读取
- 新增日期后需 `compute` 重新生成

---

## 八、HTML 风格约定

- 白底 Windows 风格，不使用暗色主题
- 字体：`"Microsoft YaHei", "Segoe UI", sans-serif`
- 标题色 `#0078d4`（蓝色），文字色 `#333`
- 图表容器背景 `#fff`，边框 `#e0e0e0`
- 配色：蓝色系（目标日）、红/绿/橙/紫（相似日）
- **配色规则：** 每条 series 的线条颜色和 tooltip 弹窗圆点颜色必须一致（`lineStyle.color` = `itemStyle.color`），参见全局 `CLAUDE.md` 的「ECharts 图表配色规则」。