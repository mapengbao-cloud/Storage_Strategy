# 竞价空间分析工作流程

## 概述

竞价空间 = 直调负荷 - (联络线受电 + 风电总加 + 光伏总加 + 核电总加 + 自备机组)，是储能电站交易策略的核心输入指标。竞价空间越大，市场可竞价电量越多，电价越有上涨压力；竞价空间越小（新能源大发时），电价越低，储能充电机会越好。

分析体系分四个层次：**预测竞价空间**（日前）→ **实际竞价空间**（事后验证）→ **预测vs实际对比**（偏差分析）→ **相似日分析**（历史规律挖掘）。

---

## 一、数据源

### 天机数据库（MySQL → 本地 SQLite）

| 本地表 | 天机源表 | 数据性质 | 96点频率 |
|-------|---------|---------|---------|
| `bidding_space_forecast` | `shandong_px_spot_dayahead_load_info` | 日前预测 | 96点/天 |
| `bidding_space_actual` | `shandong_px_spot_actual_load_info` | 实际运行 | 96点/天 |

### 同步命令

```bash
cd "06 DataMining"
python local_db.py sync --tables bidding_space --start 2026-01-01 --end 2026-06-23
```

### 数据库字段

| 本地列名 | 天机列名 | 单位 |
|---------|---------|------|
| `dispatched_load` | `dispatched_load_forecast` / `actual_dispatched_load` | MW |
| `tie_line_load` | `tie_line_load_forecast` / `actual_tie_line_load` | MW |
| `wind_power` | `wind_power_forecast` / `actual_wind_power` | MW |
| `photovoltaic_power` | `photovoltaic_power_forecast` / `actual_photovoltaic_power` | MW |
| `nuclear_power` | `nuclear_power_forecast` / `actual_nuclear_power` | MW |
| `local_power` | `local_power_forecast` / `actual_local_power` | MW |
| `self_power` | `self_power_forecast` / `actual_self_power` | MW |
| `bidding_space` | 计算列（本地计算） | MW |

---

## 二、预测竞价空间

### 生成方式一：Excel 模板（阶段 01）

**脚本：** `01 biddingSpace_analysis/generate.py`

**模板：** `01 biddingSpace_analysis/assets/输出模版-0521-竞价空间分析.xlsx`

**源文件：** `YYYY-MM-DD负荷信息预测.xls`（含 4 个 sheet：直调负荷、联络线受电负荷、风电总加、光伏总加）

**输出：** `01 biddingSpace_analysis/output/MMDD-竞价空间分析(预测).xlsx`

**用法：**
```bash
cd "01 biddingSpace_analysis"
python generate.py 0601            # 预测（自动查找源文件）
python generate.py 0601 <path>     # 显式指定源文件
```

**Excel 模板结构（Sheet1）：**
- A 列：标签（直调负荷、联络线受电负荷、风电总加、光伏总加、竞价空间）
- B-CS 列（2-97）：96 个时间点
- Row 3-6：4 个分项数据
- Row 7：竞价空间公式（`=B3-B4-B5-B6`）

> 注：Excel 源文件仅含 4 个分项（无核电/自备），故模板公式为 4 项子集。
> 完整 5 项定义（直调 − 联络线 − 风 − 光 − 核 − 自备）用于数据库路径。

**取数逻辑：** 每个 sheet 用 `pd.read_excel` 读取，`df.iloc[2, 1:]` 取 96 个值 → 写入 Row 3-6 → 公式自动计算 Row 7

### 生成方式二：ECharts 交互 HTML（阶段 06）

**脚本：** `06 DataMining/bidding_space_viz.py`

**模板：** 无（纯代码生成 HTML）

**输出：** `output/竞价空间分析结果/预测竞价空间_YYYY-MM-DD_YYYY-MM-DD.html`

**用法：**
```bash
cd "06 DataMining"
python bidding_space_viz.py                              # 预测（默认，使用脚本内 DEFAULT_START/END）
python bidding_space_viz.py 2026-06-01 2026-06-23        # 指定日期范围
python bidding_space_viz.py --local 2026-06-01 2026-06-23 # 读本地 SQLite（不走远程 MySQL）
```

**数据源：** 天机 MySQL `shandong_px_spot_dayahead_load_info` 或本地 `bidding_space_forecast`

**HTML 内容：**
- 图表 1：竞价空间曲线（左轴 MW）+ 日前电价（右轴 元/MWh）
- 图表 2：竞价空间构成（直调负荷、联络线受电、风电、光伏、核电、自备 7 条曲线）
- 侧边栏：日期选择、各分项均值和范围统计
- 键盘 ← → 切换日期

---

## 三、实际竞价空间

### 生成方式一：Excel 模板（阶段 01）

**源文件：** `MMDD-电网运行实际信息.xlsx`（sheet `负荷信息`，列式存储）

**输出：** `01 biddingSpace_analysis/output/MMDD-竞价空间分析(实际).xlsx`

**用法：**
```bash
python generate.py 0601 actual           # 自动查找
python generate.py 0601 actual <path>    # 显式指定
```

**取数逻辑：** `pd.read_excel` 读取 `负荷信息` sheet，列映射：C=直调负荷，D=联络线受电，E=风电，F=光伏（rows 1-96）

> ⚠️ 实际文件中的数值以**字符串格式**存储（如 `"62191.80"`），需用 `float()` 转换。

### 生成方式二：ECharts 交互 HTML

**用法：**
```bash
python bidding_space_viz.py --actual                          # 默认范围
python bidding_space_viz.py --actual 2026-06-01 2026-06-23    # 指定范围
```

**数据源：** `shandong_px_spot_actual_load_info` 或本地 `bidding_space_actual`

**输出：** `output/竞价空间分析结果/真实竞价空间_YYYY-MM-DD_YYYY-MM-DD.html`

---

## 四、预测 vs 实际对比（偏差分析）

**用法：**
```bash
python bidding_space_viz.py --compare                          # 默认范围
python bidding_space_viz.py --compare 2026-06-01 2026-06-23    # 指定范围
```

**输出：** `output/竞价空间分析结果/竞价空间对比_YYYY-MM-DD_YYYY-MM-DD.html`

**共 3 个图表：**
1. **竞价空间对比**：预测（虚线）vs 实际（实线）+ 日前/实时电价（右轴）
2. **偏差构成分析**：各分项偏差（实际 - 预测），含竞价空间偏差粗线
3. **各分项对比**：直调负荷、联络线受电、风电、光伏、核电、自备 7 项 × 预测/实际 2 条线

**侧边栏统计：**
- 预测竞价空间均值 / 实际竞价空间均值 / 偏差
- RMSE（均方根误差）
- 各分项平均偏差（柱状图）
- 7 分项均值对比（预测、实际、偏差）

### 偏差构成公式

竞价空间偏差 = 直调负荷偏差 - 联络线受电偏差 - 风电偏差 - 光伏偏差 - 核电偏差 - 自备偏差

通过偏差构成分析，可以定位竞价空间预测偏差的主要来源（如风电预测偏差过大、光伏预测偏差等）。

---

## 五、竞价空间相似日分析

**脚本：** `06 DataMining/similar_day_analysis.py`

**数据源：** 本地 SQLite `bidding_space_forecast`

**输出：** `output/竞价空间分析结果/相似日分析_YYYY-MM-DD.html`

### 使用流程

```bash
cd "06 DataMining"
python similar_day_analysis.py compute              # 第1步：计算所有日期的特征
python similar_day_analysis.py 2026-06-21           # 第2步：生成相似日分析 HTML
python similar_day_analysis.py 2026-06-21 --top 15  # 指定 Top N
```

### 相似度算法（5 维度加权）

| 维度 | 权重 | 说明 |
|------|------|------|
| 2h 谷值 | 30% | 2h 最低均值窗口的 MW 值 |
| 2h 峰值 | 25% | 2h 最高均值窗口的 MW 值 |
| 谷值时段 | 15% | 谷值窗口落在哪个时段（凌晨/上午/中午/下午/晚间） |
| 峰值时段 | 10% | 峰值窗口落在哪个时段 |
| 曲线形状 | 20% | Pearson 相关系数 |

### 特征提取

- **2h 滑动窗口**（8 个 15 分钟点）：遍历所有窗口，找最高/最低均值窗口
- **时段分类**：凌晨(0-5)、上午(6-11)、中午(12-13)、下午(14-17)、晚间(18-23)
- **特征存储**：`_tmp_similar_features.json`（不含完整 96 点曲线，仅存统计量）

### HTML 内容

- **图表 1**：目标日竞价空间 + 电价（点击表格行切换相似日电价）
- **图表 2**：相似日曲线叠加对比（目标日粗线，相似日细线，点击高亮）
- **图表 3**：雷达图（5 维度相似度可视化）
- **排名表**：Top N 相似日，可排序（按相似度/按日期），可点击高亮对比
- **日期跳转**：下拉框选择任意日期重新分析

---

## 六、竞价空间与电价的关系

### 核心逻辑

竞价空间直接影响电价：
- **竞价空间大** → 市场可竞价电量多 → 电价上涨压力 → 储能放电有利
- **竞价空间小**（新能源大发、负荷低谷）→ 电价低甚至负价 → 储能充电有利

### 在 ECharts HTML 中的体现

所有竞价空间 HTML 默认叠加电价曲线（右轴）：
- 预测竞价空间 → 叠加**日前电价**（`shandong_px_reliable_clearing_unit_data`，润津日前出清价）
- 实际竞价空间 → 叠加**实时电价**（`shandong_px_realtime_clearing_results_query`，润津实时出清价）
- 对比页面 → 同时叠加日前和实时电价

### 电价数据获取

| 电价类型 | 天机源表 | 本地表 | 取数字段 |
|---------|---------|--------|---------|
| 日前电价 | `shandong_px_reliable_clearing_unit_data` | `dayahead_price` | `price`，润津主单元 |
| 实时电价 | `shandong_px_realtime_clearing_results_query` | `realtime_price` | `price`，润津 member |

### 电价数据同步策略（重要）

**原则：所有本地库和天机源表的同类数据，取二者中最新的数据。**

- **天机源表优点**：每日自动更新，数据稳定可靠
- **本地表优点**：可以手动导入天机源表未更新的数据（如从交易中心下载的 Excel 文件）

**`local_db.py` 的 `auto_sync()` 函数已实现全表自动探查：**
1. 读取本地库前，先探查天机源表最新日期
2. 如果天机源表有更新（本地库缺少该日期），自动同步到本地库
3. 同步后再读取本地库，确保数据是最新的
4. `INSERT OR REPLACE` 不会覆盖本地已有数据

```bash
python "06 DataMining/local_db.py" auto-sync
```

**`similar_day_analysis.py` 的 `load_prices()` 已内置 `_auto_sync_before_load()` 调用此逻辑。**

**手动导入场景：** 当需要比天机库更早获取数据时（如 10:00 就从交易中心下载了负荷预测），可以直接写本地库，天机库自动同步不会覆盖已有数据（`INSERT OR REPLACE`）。

---

## 七、竞价空间上午/下午（两版竞价空间）

火电连续时序图表中同时展示两版竞价空间，用于对比日前预测与下午披露的差异：

### 竞价空间上午

| 项目 | 内容 |
|------|------|
| **数据源** | `shandong_px_spot_dayahead_load_info`（日前负荷预测） |
| **时间** | 日前（上午发布） |
| **公式** | `dispatched_load_forecast - tie_line_load_forecast - wind_power_forecast - photovoltaic_power_forecast - nuclear_power_forecast - self_power_forecast` |
| **含义** | 直调负荷 - 联络线 - 风电 - 光伏 - 核电 - 自备 |

### 竞价空间下午

| 项目 | 内容 |
|------|------|
| **数据源** | `shandong_px_disclosure_information_load`（信息披露负荷） |
| **时间** | 下午披露 |
| **公式** | `direct_load - tie_line_load - wind_power - photovoltaic - nuclear_power(上午) - self_power(上午)` |
| **含义** | 披露直调 - 联络线 - 风电 - 光伏 - 核电(取自上午表) - 自备(取自上午表) |

> ⚠️ **关键：** `shandong_px_disclosure_information_load` 表没有 `nuclear_power` 和 `self_power` 字段，核电和自备机组数据取自上午表 `shandong_px_spot_dayahead_load_info` 的 `nuclear_power_forecast` 和 `self_power_forecast`。

### 生成脚本

`06 DataMining/_gen_thermal_july_continuous.py`，输出 `output\火电出清_台数_电价_7月_连续时序.html`。

图表中两版竞价空间使用独立的 Y 轴：
- 竞价空间上午（绿色 `#2ca02c`）
- 竞价空间下午（紫色 `#9467bd`）

---

## 八、竞价空间与机组开机情况

竞价空间构成可以反映机组开机情况：
- **直调负荷**：全网用电需求，直接影响火电开机台数
- **风电/光伏总加**：新能源出力，新能源大发时火电压出力甚至停机
- **核电总加**：基荷，基本恒定
- **竞价空间 = 直调负荷 - 外送受电 - 新能源 - 核电 - 自备**：即火电等可竞价机组的市场空间

通过追踪竞价空间构成变化，可推断：
- 火电开机容量变化趋势
- 新能源渗透率（风电+光伏 / 直调负荷）
- 调峰压力（竞价空间谷值深度）

---

## 九、本地数据库表

| 表名 | 行数 | 说明 |
|------|------|------|
| `bidding_space_forecast` | 2,304 | 日前负荷预测（96点/天） |
| `bidding_space_actual` | 2,016 | 实际运行数据（96点/天） |
| `dayahead_price` | 2,112 | 日前电价（润津，96点/天） |
| `realtime_price` | 1,920 | 实时电价（润津，96点/天） |

---

## 十一、文件产出目录

| 类型 | 输出路径 | 生成方式 |
|------|---------|---------|
| 预测竞价空间 Excel | `01 biddingSpace_analysis/output/MMDD-竞价空间分析(预测).xlsx` | `generate.py` |
| 实际竞价空间 Excel | `01 biddingSpace_analysis/output/MMDD-竞价空间分析(实际).xlsx` | `generate.py actual` |
| 预测竞价空间 HTML | `output/竞价空间分析结果/预测竞价空间_*.html` | `bidding_space_viz.py` |
| 真实竞价空间 HTML | `output/竞价空间分析结果/真实竞价空间_*.html` | `bidding_space_viz.py --actual` |
| 对比分析 HTML | `output/竞价空间分析结果/竞价空间对比_*.html` | `bidding_space_viz.py --compare` |
| 相似日分析 HTML | `output/竞价空间分析结果/相似日分析_YYYY-MM-DD.html` | `similar_day_analysis.py` |
| 相似日特征 JSON | `_tmp_similar_features.json` | `similar_day_analysis.py compute` |
| 调频收益测算 | `output/调频收益测算/调频收益及报价测算_MMDD.xlsx` | `_gen_fm_MMDD.py` |

---

## 十一、关键操作步骤总结

### HTML 输出配色规则

所有竞价空间相关 HTML 的 ECharts 图表，每条 series 的线条颜色和 tooltip 弹窗圆点颜色必须一致（`lineStyle.color` = `itemStyle.color` = 同一色值），不允许线和点颜色不一致。参见全局 `CLAUDE.md` 的「ECharts 图表配色规则」。

### 新日期范围竞价空间可视化

1. **同步天机数据到本地**：`python local_db.py sync --tables bidding_space,price`
2. 修改 `bidding_space_viz.py` 顶部 `DEFAULT_START` / `DEFAULT_END`
3. 运行 `python bidding_space_viz.py --local`（预测）/ `--actual`（实际）/ `--compare`（对比）

> ⚠️ 每次生成可视化前都应先同步数据，确保本地库包含最新的竞价空间和价格数据。

### 相似日分析

1. **同步竞价空间** — 从天机库取最新日前负荷预测 `shandong_px_spot_dayahead_load_info`
2. **同步价格** — 从天机库取 `dayahead_price` + `realtime_price`（96 点电价）
3. `python similar_day_analysis.py compute` 计算特征
4. `python similar_day_analysis.py YYYY-MM-DD` 生成分析

> ⚠️ **重要：调用本地库前必须先从天机库同步最新数据。** 天机库数据更新有时间差，本地库不会自动刷新。相似日分析需要价格数据绘制电价曲线，如果价格没同步，相似日的电价曲线会缺失。

### 自动同步脚本

参考 `_auto_sync_0628.py`，用于定时自动执行完整流程：

```bash
python _auto_sync_0628.py
```

流程：检查天机库 → 同步竞价空间 → 同步价格 → 重算特征 → 生成 HTML

### 天机库数据更新时间

| 数据类型 | 天机库表 | 更新时间 |
|---------|---------|---------|
| 日前负荷预测 | `shandong_px_spot_dayahead_load_info` | 约 10:15 更新次日 |
| 日前电价（96点） | `shandong_px_reliable_clearing_unit_data` | 约 23:00 更新 |
| 实时电价（96点） | `shandong_px_realtime_clearing_results_query` | 约 23:00 更新 |

### 同步命令

```bash
cd "06 DataMining"
python local_db.py sync --start YYYY-MM-DD --end YYYY-MM-DD --tables bidding_space,price
```

### 竞价空间 Excel 模板生成

1. 确保源文件（`负荷信息预测.xls` 或 `电网运行实际信息.xlsx`）在 `01 biddingSpace_analysis/assets/` 中
2. 运行 `python generate.py MMDD [actual]`