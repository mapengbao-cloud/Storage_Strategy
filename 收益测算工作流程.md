# 收益测算工作流程

## 概述

三大收益测算体系：**日结算单复盘**、**实时出清收益测算**、**日前出清收益测算**。数据源来自天机数据库 MySQL（`tianrun_new`）和结算单 Excel 文件，结果存入本地 SQLite（`data/cache/local.db`），同时输出 Excel 文件到 `output/` 目录。

---

## 一、日结算单复盘

### 数据源

| 结算单类型 | 命名规则 | 大小判断 |
|-----------|---------|---------|
| 充电结算单（用电） | `6052-YYYY-MM-DD德州润津储能科技有限公司结算单-充电.xlsx` | ~11KB |
| 放电结算单（发电） | `6052-YYYY-MM-DD德州润津储能科技有限公司结算单-放电.xlsx` | ~21KB |

> 旧文件无 `-充电`/`-放电` 后缀，按大小区分：~11KB→充电（含「日清算数据」sheet），~21KB→放电（含「日清算费用」sheet）。
> 2026年7月起，结算单文件名为 `德州润津储能科技有限公司_日清明细_YYYY-MM-DD.xlsx` 格式，同样按大小区分充放电（~11KB→充电，~22KB→放电）。

### 模板（按月份和结算单版本）

| 月份 | 模板 | 充电列数 | 放电列数 |
|------|------|---------|---------|
| 1月 | `日结算收益复盘-1月.xlsx` | 28 | 37 |
| 2月 (1-5日) | `日结算收益复盘-2月.xlsx` | 28 | 37 |
| 2月 (6-28日) | `日结算收益复盘-2月新结算单.xlsx` | 30 | 42 |
| 3月 | `日结算收益复盘-3月.xlsx` | 28 | 37 |
| 4月 | `日结算收益复盘-4月.xlsx` | 28 | 37 |
| 5月 (1-24日) | `日结算收益复盘-20260525日前.xlsx` | 28 | 37 |
| 5月 (25-31日) + 6月 | `日结算收益复盘-20260525日后.xlsx` | 30 | 42 |
| 7月 | `日结算收益复盘-7月.xlsx` | 30 | 42 |

> 2026年5月25日起结算单增加 2 列（充电）和 5 列（放电），模板分「日前」和「日后」。

### 模板结构（4 sheets）

| Sheet | 说明 |
|-------|------|
| 充放测算 | 公式计算收益汇总，Row 4 为结果行（19列） |
| 充电日清算费用 | 用电结算单数据（28/30列，25行 = 24小时+合计） |
| 放电日清算费用 | 发电结算单数据（37/42列，97行 = 96时段+合计） |
| 容量分摊系数 | 容量分摊计算参数（32行） |

### 生成流程

1. 找到对应日期的充放电结算单（按大小或后缀识别）
2. `shutil.copy2(模板, 输出路径)` 复制模板
3. `openpyxl` 读取结算单 → `copy_sheet_data()` 写入对应 sheet（充电→充电日清算费用，放电→放电日清算费用）
4. `convert_to_numeric()` 确保数据为数值格式
5. 输出到 `output/日结算单收益测算/MMDD-日结算收益复盘.xlsx`

### 入库流程

1. **COM 刷新**：`win32com` 打开 Excel → `RefreshAll` + `CalculateUntilAsyncQueriesDone` → 保存
2. **openpyxl 读取**：`data_only=True` 读取「充放测算」Row 4 的 19 列计算值
3. **写入本地库**：

| 本地表名 | 数据来源 | 行数/天 |
|---------|---------|---------|
| `日结算单收益测算` | 充放测算 Row 4（19列） | 1 |
| `用电结算单` | 充电日清算费用 Row 5-29（28/30列） | 25 |
| `发电结算单` | 放电日清算费用 Row 5-101（37/42列） | 97 |

> 旧模板（28/37列）入库时自动补齐缺失列为 NULL，以匹配新模板（30/42列）的列结构。

---

## 二、实时出清收益测算

### 天机数据库源表

`shandong_px_realtime_clearing_results_query`（天机 MySQL，远程直连）

| 关键字段 | 说明 |
|---------|------|
| `date` | 日期 |
| `time_point` | 时间点（96点，00:15~24:00） |
| `power` | 出力（列8）→ 充放电曲线 |
| `price` | 电价（列10）→ 统一结算实时 + 节点实时 |

> 只取润津（`member_id = 'b9e64e64a713458eba94c9af05c0a757'`），96 点/天。

### 模板（按月份）

| 月份 | 模板 |
|------|------|
| 1月 | `实时出清收益测算-1月.xlsx` |
| 2月 | `实时出清收益测算-2月.xlsx` |
| 3月 | `实时出清收益测算-3月.xlsx` |
| 4月 | `实时出清收益测算-4月.xlsx` |
| 5月 | `实时出清收益测算-5月.xlsx` |
| 6月 | `实时出清收益测算-6月.xlsx` |

> 每月模板参数不同（H12、H13 等月度均值和分摊系数），必须严格按月份对齐。

### 模板结构（3 sheets）

| Sheet | 更新列 | 说明 |
|-------|-------|------|
| 报价及预中标 | H, J, K, N（Row 2-97） | H=时间戳，J/K=price，N=power |
| 充放测算 | 公式自动计算 | Row 4 为结果行（17列） |
| 容量分摊系数 | 无需更新 | 模板自带参数 |

### 生成流程

1. **直接从天机 MySQL 查询** 96 点数据（`time_point, power, price`），不经过本地库
   ```sql
   SELECT date, time_point, power, price
   FROM shandong_px_realtime_clearing_results_query
   WHERE date = 'YYYY-MM-DD' AND member_id = 'b9e64e64a713458eba94c9af05c0a757'
   ORDER BY time_point
   ```
2. 按月份匹配模板 → `shutil.copy2` 复制
3. 写入「报价及预中标」sheet：H=时间戳，J/K=price，N=power
4. 输出到 `output/实时出清收益测算/MMDD-实时出清收益测算.xlsx`
5. **同步原始数据到本地库** `润津实时出清结果`（全字段 INSERT OR REPLACE）

### 入库流程

1. COM 刷新所有文件
2. 读取「充放测算」Row 4 的 17 列计算值
3. 写入 `实时出清收益测算` 表（17列，列名=Row 3）

> **重要：** 生成 Excel 时必须从**天机远程 MySQL** 直接取数，不能从本地 `润津实时出清结果` 取数，因为本地库可能未同步最新数据。生成 Excel 后再同步原始数据到本地库。

---

## 三、日前出清收益测算

### 天机数据库源表

`shandong_px_reliable_clearing_unit_data`（天机 MySQL，远程直连）

| 关键字段 | 说明 |
|---------|------|
| `date` | 日期 |
| `time_point` | 时间点（96点） |
| `power` | 出力（列8）→ 充放电曲线 |
| `price` | 电价（列10）→ 统一结算日前 + 节点日前 |

> 只取润津主单元（`unit_name` 不含「发电」/「用电」后缀），`member_id = 'b9e64e64a713458eba94c9af05c0a757'`，96 点/天。

### 模板（按月份）

| 月份 | 模板 |
|------|------|
| 1月 | `日前出清收益测算-1月.xlsx` |
| 2月 | `日前出清收益测算-2月.xlsx` |
| 3月 | `日前出清收益测算-3月.xlsx` |
| 4月 | `日前出清收益测算-4月.xlsx` |
| 5月 | `日前出清收益测算-5月.xlsx` |
| 6月 | `日前出清收益测算-6月.xlsx` |

> 每月模板参数不同，必须严格按月份对齐。

### 模板结构（3 sheets，与实时相同）

| Sheet | 更新列 | 说明 |
|-------|-------|------|
| 报价及预中标 | H, J, K, N（Row 2-97） | J=统一结算日前，K=节点日前 |
| 充放测算 | 公式自动计算 | Row 4 为结果行（17列） |
| 容量分摊系数 | 无需更新 | 模板自带参数 |

### 生成流程

1. **直接从天机 MySQL 查询** 96 点数据（`time_point, power, price`），不经过本地库
   ```sql
   SELECT date, time_point, power, price
   FROM shandong_px_reliable_clearing_unit_data
   WHERE date = 'YYYY-MM-DD' AND member_id = 'b9e64e64a713458eba94c9af05c0a757'
     AND unit_name NOT LIKE '%发电%' AND unit_name NOT LIKE '%用电%'
   ORDER BY time_point
   ```
2. 按月份匹配模板
3. 写入「报价及预中标」sheet
4. 输出到 `output/日前出清收益测算/MMDD-日前出清收益测算.xlsx`
5. **同步原始数据到本地库** `润津日前出清结果`（全字段 INSERT OR REPLACE）

### 入库流程（与实时相同）

1. COM 刷新 → openpyxl 读取「充放测算」Row 4
2. 写入 `日前出清收益测算` 表（17列）

> **重要：** 生成 Excel 时必须从**天机远程 MySQL** 直接取数，不能从本地 `润津日前出清结果` 取数，因为本地库可能未同步最新数据。生成 Excel 后再同步原始数据到本地库。

---

## 四、本地数据库总览

| 数据库 | 路径 |
|-------|------|
| SQLite | `data/cache/local.db` |

| 表名 | 行数 | 说明 |
|------|------|------|
| `日结算单收益测算` | 170 | 日结算复盘（充放测算 Row 4） |
| `用电结算单` | 4,250 | 充电日清算费用（25行/天） |
| `发电结算单` | 16,490 | 放电日清算费用（97行/天） |
| `实时出清收益测算` | 173 | 实时出清收益（17列） |
| `日前出清收益测算` | 96 | 日前出清收益（17列） |
| `润津实时出清结果` | 16,608 | 天机实时出清原始数据 |
| `润津日前出清结果` | 9,216 | 天机日前出清原始数据 |

---

## 五、关键操作步骤总结

### 新月份日结算复盘

1. 确认结算单文件已放入 `04 Daily_Settlement_Review/assets/`
2. 确认模板已放入 `assets/templates/`
3. 运行生成脚本 → `output/日结算单收益测算/`
4. COM 刷新所有文件
5. 入库 3 张表

### 新月份实时/日前出清收益测算

1. 确认天机数据库有当月数据（`润津实时出清结果` / `润津日前出清结果`）
2. 确认模板已放入 `assets/templates/`
3. 运行生成脚本 → `output/实时出清收益测算/` / `output/日前出清收益测算/`
4. COM 刷新所有文件
5. 入库

### COM 刷新（必须步骤）

源文件 Row 4 全是公式，openpyxl 无法计算，必须用 `win32com` 调用 Excel 引擎打开、计算、保存，之后 `openpyxl(data_only=True)` 才能读到缓存值。

```python
import win32com.client, pythoncom
excel = win32com.client.Dispatch('Excel.Application', pythoncom.CoInitialize())
excel.Visible = False; excel.DisplayAlerts = False
wb = excel.Workbooks.Open(path)
wb.RefreshAll(); excel.CalculateUntilAsyncQueriesDone()
wb.Save(); wb.Close()
excel.Quit()
```

---

## 六、日结算单收益测算周报

### 数据来源

本地 SQLite `data/cache/local.db` → `日结算单收益测算` 表（19 列，与日结算复盘「充放测算」Row 4 的 19 列一一对应）。

### 模板

以上一周的周报作为模板。例如生成 0615-0621 周报时，复制 `0613-0619` 的周报文件作为模板。

### 模板结构（1 sheet）

| Sheet | 说明 |
|-------|------|
| 日结算单收益测算 | A1:T9，Row 1 表头，Row 2-8 每日数据，Row 9 合计公式 |

| 列 | 表头 | 数据来源（DB 字段） |
|----|------|---------------------|
| A | 日期 | `date`（MMDD 字符串） |
| B | 充电电价(元/MWh) | `充电电价_元每MWh` |
| C | 充电量(MWh) | `充电量_MWh` |
| D | 交易充电费(元) | `交易充电费_元` |
| E | 日前分摊(元) | `日前分摊_元` |
| F | 容量补偿(元) | `容量补偿_元` |
| G | 调频分摊(元) | `调频分摊_元` |
| H | 系统倒送(元) | `系统倒送_元` |
| I | 容量分摊(元) | `容量分摊_元` |
| J | 容量分摊系数 | `容量分摊系数` |
| K | 放电电价(元/MWh) | `放电电价_元每MWh` |
| L | 放电量(MWh) | `放电量_MWh` |
| M | 放电费(元) | `放电费_元` |
| N | 综合效率 | `综合效率` |
| O | 充放电价差(元/MWh) | `充放电价差_元每MWh` |
| P | 结算单费用合计 | `结算单费用合计` |
| Q | 实时市场充放合计 | `实时市场充放合计` |
| R | 调频结算分摊合计 | `调频结算分摊合计` |
| S | 结算单合计 | `结算单合计` |
| T | 净收益 | `净收益` |

### Row 9 合计公式

| 列 | 公式 | 说明 |
|----|------|------|
| B9 | `=D9/C9` | 加权平均充电电价 |
| C9 | `=SUM(C2:C8)` | 总充电量 |
| D9 | `=SUM(D2:D8)` | 总交易充电费 |
| K9 | `=M9/L9` | 加权平均放电电价 |
| L9 | `=SUM(L2:L8)` | 总放电量 |
| M9 | `=SUM(M2:M8)` | 总放电费 |
| O9 | `=K9-B9` | 充放电价差 |
| P9 | `=SUM(P2:P8)` | 周结算单费用合计 |
| Q9 | `=SUM(Q2:Q8)` | 周实时市场充放合计 |
| S9 | `=SUM(S2:S8)` | 周结算单合计 |
| T9 | `=SUM(T2:T8)` | 周净收益 |

### 生成流程

1. 确认 `local.db` 中已有目标日期范围的日结算数据（`日结算单收益测算` 表）
2. 复制上周周报模板 → `output/日结算单收益测算_周报_MMDD-MMDD.xlsx`
3. openpyxl 打开，逐行写入 Row 2-8 的 A-T 列数据（A 列写日期字符串，B-T 列写 DB 数值）
4. Row 9 公式已存在于模板中，无需修改，Excel 打开后自动计算
5. 生成文本周报 → `output/日结算单收益测算_周报_MMDD-MMDD.txt`
6. 生成竞价空间综合分析 HTML → `output/日结算单收益测算/周报/竞价空间_电价_天气综合分析_MMDD-MMDD.html`

> 竞价空间 HTML 由 `_gen_weekly_report.py` 的 `generate_analysis_html()` 函数自动生成，流程：本地库取竞价空间+电价 → 天机取充放电功率 → Open-Meteo 取天气 → 生成 `_tmp_html_data.json` → 调用 `generate_analysis_html.py` 生成 HTML。

### 脚本

`04 Daily_Settlement_Review/_gen_weekly_report.py`，修改 `DATES` 和 `RANGE_LABEL` 后运行即可。会自动生成周报 Excel、文本周报、竞价空间 HTML 三个文件。

### 文本周报格式

```
MMDD-MMDD
上周收益：P9/10000万（其中过网费、容量分摊等|S9|/10000万）
实时市场：Q9/10000万（充电量：|C9|MWh；放电量：L9MWh；价差：O9元/MWh）
日前套利：0万
调频补偿：0万元（中标时段：个，中标均价：，性能：）

数据来源：日结算单收益测算_周报_MMDD-MMDD.xlsx
```

> 周报中 P9/Q9/S9 为 Row 9 公式计算值（万元），C9/L9 为合计值，O9 为加权价差。DB 中充放电量/充电费为负值，文本中取绝对值显示。

---

## 七、文件产出目录

| 类型 | 输出路径 |
|------|---------|
| 日结算复盘 | `output/日结算单收益测算/MMDD-日结算收益复盘.xlsx` |
| 月度汇总 | `output/日结算单收益测算/月度日结算收益复盘汇总_2026年X月.xlsx` |
| 实时出清 | `output/实时出清收益测算/MMDD-实时出清收益测算.xlsx` |
| 日前出清 | `output/日前出清收益测算/MMDD-日前出清收益测算.xlsx` |
| 周报 Excel | `output/日结算单收益测算/周报/日结算单收益测算_周报_MMDD-MMDD.xlsx` |
| 周报文本 | `output/日结算单收益测算/周报/日结算单收益测算_周报_MMDD-MMDD.txt` |
| 竞价空间 HTML | `output/日结算单收益测算/周报/竞价空间_电价_天气综合分析_MMDD-MMDD.html` |